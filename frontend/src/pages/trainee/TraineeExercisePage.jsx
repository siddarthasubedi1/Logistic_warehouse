import { useState } from "react"; import { useParams } from "react-router-dom"; import DashboardLayout from "../../components/dashboard/DashboardLayout"; import singleTruck from "../../images/single-truck.jpg"; import "./Training360Flow.css";
export default function TraineeExercisePage() { const { programmeId } = useParams(); const [found, setFound] = useState([]); const toggle = n => setFound(v => v.includes(n) ? v : [...v, n]); return <DashboardLayout role="trainee" title="Start Exercise" subtitle="Identify the hazards in this workplace scene."><section className="exercise360"><div className="exercise360__scene"><img src={singleTruck} alt="Warehouse safety exercise" /><button className="exercise360__hazard h1" onClick={() => toggle(1)}>!</button><button className="exercise360__hazard h2" onClick={() => toggle(2)}>!</button><button className="exercise360__hazard h3" onClick={() => toggle(3)}>!</button></div><aside className="exercise360__panel"><span>Interactive exercise</span><h2>Find the hazards</h2><p>Click the highlighted areas to identify hazards in the scene.</p><strong>{found.length} / 3 hazards identified</strong><div className="exercise360__progress"><i style={{ width: `${found.length / 3 * 100}%` }} /></div><button onClick={() => location.href = `/my-training/${programmeId}`}>Back to Learning Content</button></aside></section></DashboardLayout> }
